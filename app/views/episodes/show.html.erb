<h1>Episode number: <%= @episode.number %> || <%= @episode.date %></h1>
<h3>Guest List</h3>
<% @episode.guests.uniq.each do |guest| %>
<ul>
<li><%= link_to guest.name, guest_path(guest) %></li>
</ul>
<% end %>
<!-- I ran out of time but this is what I was thinking about for the average rating
 @episode.appearences.each do |appearence| 
  num_1 = appearence.rating.size %>
 num_2 = appearence.rating.sum %> 
 Avg Rating: num_2/ num_1  
end -->
<%= button_to "ALL EPISODES", {:action => "index"}, :method => "get" %>